# example-nonconforming pack

Fixture pack used by `tests/test_validate_pack.py` as the **negative control** for the
pack conformance gate. It is deliberately broken; see `tests/fixtures/_index.md` for the
exact list of violations it is expected to raise.
